import { createFileRoute, Link } from "@tanstack/react-router";import { PageHero } from "@/components/PageHero";import { SocialLinks } from "@/components/SocialLinks";import { Button } from "@/components/ui/button";import { categories, overviewItems, standaloneTreatments } from "@/lib/site-data";import { canonical,pageMeta } from "@/lib/seo";
export const Route=createFileRoute("/kozmetikai-kezelesek/")({head:()=>({meta:pageMeta({title:"Kozmetikai kezelések | Erika Beauty Kozmetika",description:"Az Erika Beauty Kozmetika kezelései Budapesten, a XII. kerületben.",path:"/kozmetikai-kezelesek",image:"/images/hero-treatments.png"}),links:canonical('/kozmetikai-kezelesek')}),component:Page});
function Page(){return <><PageHero src="/images/hero-treatments.png" alt="Erika Beauty kozmetikai kezelések" eager/><section className="mx-auto max-w-7xl px-4 py-16 sm:px-6"><h1 className="text-center font-display text-3xl font-semibold text-primary sm:text-4xl">Kozmetikai kezelések</h1><SocialLinks className="mt-4 justify-center"/><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{categories.map(c=><Link key={c.slug} to="/kozmetikai-kezelesek/$category" params={{category:c.slug}} className="border-b border-primary/40 bg-secondary p-6"><h2 className="font-display text-2xl font-semibold">{c.name}</h2><p className="mt-3 text-sm text-muted-foreground">{c.lead}</p></Link>)}{standaloneTreatments.map(t=><Link key={t.slug} to="/kozmetikai-kezelesek/$standalone" params={{standalone:t.slug}} className="border-b border-primary/40 bg-secondary p-6"><h2 className="font-display text-2xl font-semibold">{t.name}</h2><p className="mt-3 text-sm text-muted-foreground">Időtartam: {t.duration}</p></Link>)}</div><h2 className="mt-20 font-display text-3xl font-semibold">Szolgáltatások és időtartamok</h2><div className="mt-7 divide-y divide-border border-y border-border">{overviewItems.map(i=><div key={i.name} className="grid gap-2 py-4 sm:grid-cols-[1fr_auto]">{i.to?<Link to={i.to} className="font-medium hover:text-primary">{i.name}</Link>:<span className="font-medium">{i.name}</span>}<span className="text-muted-foreground">{i.duration}</span>{i.note&&<p className="text-sm text-muted-foreground sm:col-span-2">{i.note}</p>}</div>)}</div><div className="mt-10"><Button asChild><Link to="/foglalas">Időpontfoglalás</Link></Button></div></section></>};